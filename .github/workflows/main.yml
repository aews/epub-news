name: heise

on:
  workflow_dispatch:
#  push:
#    branches:
#      - "main"

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: alexanderwessel/calibre    
    steps:
      -
        name: Checkout
        uses: actions/checkout@v4
      - run:  bash publish-heise-to-dropbox.sh