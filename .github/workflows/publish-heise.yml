on:
  workflow_dispatch:
#  push:
#    branches:
#      - "main"

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: alexanderwessel/calibre    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: publish-heise-to-dropbox
        env:
            DROPBOX_PERSONAL_TOKEN: ${{ secrets.DROPBOX_PERSONAL_TOKEN }}
        run: |
            bash publish-heise-to-dropbox.sh